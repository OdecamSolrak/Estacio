#include <stdio.h>
int main(){
    //tipo da variável, nome da variável e valor da variável

    int idade = 37;//int = número inteiro. tipo = int, nome = idade. valor = 37
    float altura = 1.84;//float = números com casas decimais
    double medida = 84.37;//double = números com casas decimais com mais precisão (mais casas decimais)
    char letra = 'F';//char = apenas uma letra (string)
    char palavra[9] = 'Fernando';//para ecrever uma palavra ou texto é necessario a colocação dos [] e neles inseridos no mínimo a númeração que representa a quantidade de letras que essa palavra ou texto possui somado mais 1(o espaço entre as palavras também são contabilizados)

    //A inicialização só ocorre quando se determina o valor da variável
}